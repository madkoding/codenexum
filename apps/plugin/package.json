{
  "name": "@codenexum/plugin",
  "version": "0.99.2",
  "description": "opencode plugin — thin MCP client to the CodeNexum Electron app",
  "type": "module",
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "build": "tsc",
    "bundle": "bun build ./dist/index.js --target=bun --format=esm --outfile=./dist/index.bundled.js --external none"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.17.18"
  }
}
